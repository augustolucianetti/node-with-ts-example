# node-with-ts-example

  To build and run:
  
  Compile Typescript: npm run tsc
  
  Run: node build/app.js

  To run without tranpiling:

    In developer mode:

    npm run dev

   To run in production mode:

    npm run prod